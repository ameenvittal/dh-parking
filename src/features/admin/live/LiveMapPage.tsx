import { PageFallback } from '@/app/PageFallback'

/** Stub created by the router (Chat 1). The owning chat replaces this file completely. */
export function LiveMapPage() {
  return <PageFallback />
}
